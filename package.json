{
  "name": "inventory-manager-crm",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "concurrently -n server,client \"npm start --prefix server\" \"npm start --prefix client\"",
    "setup": "cd server && npm install && cd ../client && npm install --legacy-peer-deps"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "ts-pattern": "^5.9.0",
    "uuid": "^14.0.0",
    "zod": "^4.4.3"
  }
}
